export default function EditLabsPage({ params }: { params: { id: number } }) {
    return (
        <div className="min-h-screen">
            {/* <FormEditRole roleId={params.id} /> */}
        </div>
    );
}

